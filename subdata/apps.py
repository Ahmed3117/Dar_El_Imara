from django.apps import AppConfig


class SubdataConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'subdata'
    verbose_name = '  البنود والمنتجات '
    # order = 4
